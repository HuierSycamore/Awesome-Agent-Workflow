# [US编号] 任务概览

## 元信息
- US编号：[US-XXXX]
- 来源设计文档：[路径]
- 任务总数：[N]
- 生成时间：[YYYY-MM-DD]

## 执行规则
- 按前置任务字段确定的拓扑序逐个执行
- 同一时刻只能开发一个任务，不允许并行
- 多个可开工任务按编号小者优先
- 任务编号只作标识，不保证等于执行顺序
- 每个任务必须通过其 DoD checklist 才能开始下一个任务；唯一例外：未通过项是测试用例且该用例标了 `依赖任务: T[X]`，该用例挂账登记、不阻断下一个任务，待 T[X] 完成后回归

## 任务概览

| 编号 | 任务标题 | 代码影响模块 | 前置任务 | 状态 |
|-----|---------|------------|---------|-----|
| T1  | [任务标题] | [模块名] | [无 / T编号] | Pending |
| T2  | [任务标题] | [模块名] | [无 / T编号] | Pending |
| T3  | [任务标题] | [模块名] | [无 / T编号] | Pending |

## 任务依赖链

每行一条 `前置 → 后置` 关系，如实表达 DAG；无前置任务的起点任务不必列出。

示例：
```
T1 → T3
T2 → T3
T3 → T4
```

## 存疑汇总（Open Questions）

（如果各 task 或设计文档中有未解决的存疑，在此汇总）

| 编号 | 存疑项 | 涉及任务 | 需要确认的问题 |
|-----|-------|---------|--------------|
| Q1  | [存疑描述] | T1, T2 | [待确认问题] |

## 挂账用例登记

（仅当存在标了 `依赖任务` 的用例时填写；无则保留此节为空）

| 用例编号 | 所在任务 | 依赖任务 | 状态 |
|---------|---------|---------|-----|
| TC-T[N]-0X | T[N] | T[X] | Pending |

状态取值：`Pending`（挂账待回归）/ `Passed`（回归通过）/ `Failed`（回归失败）
